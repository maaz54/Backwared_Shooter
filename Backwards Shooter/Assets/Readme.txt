* There an scriptable object name LevelSetting in assets folder you can create and Update levels.
* There is an GameObject name level contains componenet name level it contains a property LevelNo
create the levels by pressing 'L' and delete it by pressing 'D' at runtime.